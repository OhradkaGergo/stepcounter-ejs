<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand">Menü - <% if (session.user) { %> <%= session.user.name %> <% } else { %> X <% } %></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <!-- always --> 
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>

                <!-- if logged out -->
                <% if (!session.user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/users/login">Belépés</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="/users/registration">Regisztráció</a>
                    </li>
                <% } %>

                <!-- if logged in -->
                <% if (session.user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/steps">Lépések</a>
                    </li>
                    <!--
                    <li class="nav-item">
                        <a class="nav-link" href="/tasks/calendar">Naptár</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="/tasks/statistics">Statisztika</a>
                    </li>
                    -->
                    <li class="nav-item">
                        <a class="nav-link" href="/users/logout">Kilépés</a>
                    </li>
                    
                <% } %>

            </ul>
        </div>
    </div>
</nav>